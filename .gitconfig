[core]
  pager = delta
[interactive]
  diffFilter = delta --color-only
[delta]
  navigate = true
  dark = true
[merge]
  tool = nvimdiff

[user]
	email =
	name =
[alias]
  gdc = !git diff --name-only --relative --diff-filter=d -z | xargs -0 batcat --diff

  
[pull]
	rebase = false
